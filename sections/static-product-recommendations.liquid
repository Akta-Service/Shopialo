{% if section.settings.show_product_recommendations %}
  {% assign product_limit = 5 %}

  <script
    type="application/json"
    data-section-type="static-product-recommendations"
    data-section-id="{{ section.id }}"
    data-section-data
    >
{
  "settings": {
    "limit": {{ product_limit }}
  },
    "productId": {{ product.id | json }},
    "sectionId": {{ section.id | json }}
}
  </script>

  <!-- OPTIMIZATION: CSS skeleton for CLS=0 - Recommendations load via observer in Batch 4 -->
  <section
    class="product-section--container product-row--container product-recommendations--container"
    data-html
    data-product-recommendations
    data-observer-section="recommendations"
    >
    {% if recommendations.products_count > 0 %}
      <h2 class="product-section--title product-recommendations--title">
        {{ section.settings.product_recommendations_heading }}
      </h2>

    <!-- OPTIMIZATION: Skeleton grid for instant paint with CLS=0 -->
    <div class="product-section--content product-row" data-skeleton-grid style="display:grid;">
      {% for i in (1..product_limit) %}
        <div class="skeleton-product-card">
          <div class="skeleton-product-image"></div>
          <div class="skeleton-product-title"></div>
          <div class="skeleton-product-price"></div>
        </div>
      {% endfor %}
    </div>

    <!-- OPTIMIZATION: Real products hidden until observer loads them -->
    <div
      class="product-section--content product-row"
      data-product-row
      style="display:none;"
      >
      {% for product in recommendations.products limit: product_limit %}
        {%
          render 'product-grid-item',
          product: product,
          lazy_images: true
        %}
      {% endfor %}
    </div>
    {% endif %}
  </section>

  {% render 'quick-shop' %}
{% endif %}

{% schema %}
{
  "name": "t:sections.product_recommendations.name",
  "class": "product-recommendations--section",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_product_recommendations",
      "label": "t:sections.product_recommendations.show_product_recommendations.label",
      "info": "t:sections.product_recommendations.show_product_recommendations.info",
      "default": true
    },
    {
      "type": "text",
      "id": "product_recommendations_heading",
      "label": "t:sections.product_recommendations.product_recommendations_heading.label",
      "default": "You may also like"
    }
  ]
}
{% endschema %}